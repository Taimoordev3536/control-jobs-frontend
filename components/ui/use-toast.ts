// This file exists only as a re-export for backwards compatibility.
// The single source of truth for the toast system is `@/hooks/use-toast`.
// Do not duplicate the implementation here — import from `@/hooks/use-toast` directly.
export { useToast, toast } from "@/hooks/use-toast"
